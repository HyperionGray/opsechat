name: "AmazonQ Review after GitHub Copilot"

on:
  # Triggered on every push
  push:
    branches:
      - main
      - master
      - develop
  # Triggered when GitHub Copilot workflows complete
  workflow_run:
    workflows:
      - "Periodic Code Cleanliness Review"
      - "Comprehensive Test Review with Playwright"
      - "Code Functionality and Documentation Review"
      - "Org-wide: Copilot Playwright Test, Review, Auto-fix, PR, Merge"
      - "Complete CI/CD Agent Review Pipeline"
    types:
      - completed
  workflow_dispatch:
    inputs:
      ai_model:
        description: 'AI Model to use for review'
        required: false
        default: 'amazonq'
        type: choice
        options:
          - amazonq
          - codex
          - gemini
          - gpt5

permissions:
  contents: write
  pull-requests: write
  issues: write
  actions: read

jobs:
  wait-for-copilot-agents:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' || github.event_name == 'workflow_dispatch' }}
    steps:
      - name: Checkout code
        uses: actions/checkout@main

      - name: Wait for any pending Copilot PRs
        uses: actions/github-script@main
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            // Wait a bit for Copilot agents to potentially create PRs
            console.log('Waiting for Copilot agents to complete...');
            await new Promise(resolve => setTimeout(resolve, 30000)); // 30 second delay
            
            // Check for recent Copilot PRs
            const prs = await github.rest.pulls.list({
              owner: context.repo.owner,
              repo: context.repo.repo,
              state: 'open',
              sort: 'created',
              direction: 'desc',
              per_page: 10
            });
            
            const copilotPRs = prs.data.filter(pr => 
              pr.title.includes('Copilot') || 
              pr.head.ref.includes('copilot') ||
              pr.user.login === 'github-actions[bot]'
            );
            
            if (copilotPRs.length > 0) {
              console.log(`Found ${copilotPRs.length} recent Copilot PRs`);
              copilotPRs.forEach(pr => {
                console.log(`  - PR #${pr.number}: ${pr.title}`);
              });
            } else {
              console.log('No recent Copilot PRs found');
            }

  amazonq-code-review:
    runs-on: ubuntu-latest
    needs: wait-for-copilot-agents
    steps:
      - name: Checkout code
        uses: actions/checkout@main
        with:
          fetch-depth: 0

      - name: Setup AWS credentials for Amazon Q
        uses: aws-actions/configure-aws-credentials@main
        with:
          aws-region: us-east-1
          # Note: AWS credentials should be configured in repository secrets
          # AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY
        continue-on-error: true

      - name: Prepare code for Amazon Q review
        id: prepare
        run: |
          echo "## Amazon Q Code Review Preparation" > /tmp/amazonq-prep.md
          echo "" >> /tmp/amazonq-prep.md
          echo "Repository: ${{ github.repository }}" >> /tmp/amazonq-prep.md
          echo "Branch: ${{ github.ref_name }}" >> /tmp/amazonq-prep.md
          echo "Triggered by: ${{ github.event.workflow_run.name || 'Manual trigger' }}" >> /tmp/amazonq-prep.md
          echo "" >> /tmp/amazonq-prep.md
          
          # Get list of recent changes
          echo "### Recent Changes:" >> /tmp/amazonq-prep.md
          git log --oneline -10 >> /tmp/amazonq-prep.md || echo "No recent commits" >> /tmp/amazonq-prep.md
          
          echo "" >> /tmp/amazonq-prep.md
          echo "### Files Changed Recently:" >> /tmp/amazonq-prep.md
          git diff --name-only HEAD~5..HEAD 2>/dev/null >> /tmp/amazonq-prep.md || echo "No changes in last 5 commits" >> /tmp/amazonq-prep.md
          
          cat /tmp/amazonq-prep.md

      - name: Setup Python for Amazon Q integration
        uses: actions/setup-python@main
        with:
          python-version: '3.11'

      - name: Install Amazon Q integration dependencies
        run: |
          python -m pip install --upgrade pip
          pip install boto3 botocore pathlib

      - name: Run Amazon Q Code Review
        id: amazonq
        run: |
          echo "Running Amazon Q code review..."
          
          # Use the new Amazon Q integration module
          python amazon_q_integration.py . > /tmp/amazonq-output.txt 2>&1 || true
          
          # Check if the integration ran successfully
          if [ -f "amazon_q_review_results.json" ]; then
            echo "Amazon Q integration completed successfully"
            
            # Extract key information from results
            python -c "
import json
import sys

try:
    with open('amazon_q_review_results.json', 'r') as f:
        results = json.load(f)
    
    print('## Amazon Q Code Review Results')
    print('')
    print(f'**Overall Score:** {results.get(\"overall_score\", \"N/A\")}/100')
    print(f'**Service Used:** {results.get(\"service_used\", \"Unknown\")}')
    print(f'**Timestamp:** {results.get(\"timestamp\", \"Unknown\")}')
    
    if results.get('mock_mode'):
        print('**Mode:** Mock Analysis (AWS services not available)')
    
    print('')
    
    # Security analysis
    security = results.get('security_analysis', {})
    print('### Security Analysis')
    print(f'- Files scanned: {security.get(\"total_files_scanned\", 0)}')
    print(f'- Vulnerabilities found: {security.get(\"vulnerabilities_found\", 0)}')
    print('')
    
    # Code quality
    quality = results.get('code_quality', {})
    metrics = quality.get('metrics', {})
    print('### Code Quality Metrics')
    for metric, value in metrics.items():
        print(f'- {metric.replace(\"_\", \" \").title()}: {value}')
    print('')
    
    # Architecture
    architecture = results.get('architecture_review', {})
    print('### Architecture Analysis')
    print(f'- Architecture Score: {architecture.get(\"architecture_score\", \"N/A\")}')
    
    structure = architecture.get('structure', {})
    if structure:
        print(f'- Total Files: {structure.get(\"total_files\", \"N/A\")}')
        print(f'- Has README: {\"✅\" if structure.get(\"has_readme\") else \"❌\"}')
        print(f'- Has License: {\"✅\" if structure.get(\"has_license\") else \"❌\"}')
    print('')
    
    # Recommendations
    recommendations = results.get('recommendations', [])
    if recommendations:
        print('### Recommendations')
        for i, rec in enumerate(recommendations[:3], 1):  # Limit to first 3
            priority_emoji = '🔴' if rec.get('priority') == 'high' else '🟡' if rec.get('priority') == 'medium' else '🟢'
            print(f'{i}. {priority_emoji} **{rec.get(\"title\", \"Unknown\")}** ({rec.get(\"priority\", \"unknown\")} priority)')
            print(f'   {rec.get(\"description\", \"No description\")}')
        print('')
    
    # Add integration status
    if results.get('mock_mode'):
        print('### Integration Status')
        print('⚠️ **Mock Mode Active**: AWS credentials not configured or services unavailable')
        print('- To enable full Amazon Q integration, configure AWS credentials in repository secrets')
        print('- Add AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY to repository secrets')
        print('- Ensure proper IAM permissions for CodeWhisperer and Bedrock services')
    else:
        print('### Integration Status')
        print('✅ **Full Integration Active**: Amazon Q services are available and configured')
    
except Exception as e:
    print(f'Error processing Amazon Q results: {e}')
    print('## Amazon Q Integration Error')
    print('Failed to process review results. Check the integration configuration.')
" > /tmp/amazonq-report.md
          else
            echo "Amazon Q integration failed, creating fallback report..."
            
            # Create fallback report
            echo "## Amazon Q Code Review Report" > /tmp/amazonq-report.md
            echo "" >> /tmp/amazonq-report.md
            echo "**Review Date:** $(date -u +"%Y-%m-%d %H:%M:%S UTC")" >> /tmp/amazonq-report.md
            echo "" >> /tmp/amazonq-report.md
            echo "### Integration Status" >> /tmp/amazonq-report.md
            echo "❌ **Integration Failed**: Unable to run Amazon Q code review" >> /tmp/amazonq-report.md
            echo "" >> /tmp/amazonq-report.md
            echo "**Possible Issues:**" >> /tmp/amazonq-report.md
            echo "- AWS credentials not configured" >> /tmp/amazonq-report.md
            echo "- Missing dependencies" >> /tmp/amazonq-report.md
            echo "- Network connectivity issues" >> /tmp/amazonq-report.md
            echo "" >> /tmp/amazonq-report.md
            echo "**Setup Instructions:**" >> /tmp/amazonq-report.md
            echo "1. Add AWS_ACCESS_KEY_ID to repository secrets" >> /tmp/amazonq-report.md
            echo "2. Add AWS_SECRET_ACCESS_KEY to repository secrets" >> /tmp/amazonq-report.md
            echo "3. Ensure IAM permissions for CodeWhisperer and Bedrock" >> /tmp/amazonq-report.md
            echo "" >> /tmp/amazonq-report.md
            echo "**Error Output:**" >> /tmp/amazonq-report.md
            echo '```' >> /tmp/amazonq-report.md
            cat /tmp/amazonq-output.txt >> /tmp/amazonq-report.md 2>/dev/null || echo "No error output available" >> /tmp/amazonq-report.md
            echo '```' >> /tmp/amazonq-report.md
          fi
          
          # Display the report
          cat /tmp/amazonq-report.md
        continue-on-error: true

      - name: Create Amazon Q Review Issue
        uses: actions/github-script@main
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const fs = require('fs');
            const report = fs.readFileSync('/tmp/amazonq-report.md', 'utf8');
            
            const date = new Date().toISOString().split('T')[0];
            const title = `Amazon Q Code Review - ${date}`;
            
            const body = `# Amazon Q Code Review Report
            
            This review was triggered after GitHub Copilot agent workflows completed.
            
            ${report}
            
            ## Review Context
            
            - **Triggered by:** ${{ github.event.workflow_run.name || 'Manual workflow dispatch' }}
            - **Repository:** ${{ github.repository }}
            - **Branch:** ${{ github.ref_name }}
            - **Commit:** ${{ github.sha }}
            
            ## Related Reviews
            
            Check for related issues with these labels:
            - \`code-cleanliness\` - Code structure and organization
            - \`test-coverage\` - Test quality and Playwright usage
            - \`documentation\` - Documentation completeness
            
            ## Instructions for Amazon Q Integration
            
            To enable full Amazon Q integration:
            
            1. **Set up AWS credentials** in repository secrets:
               - \`AWS_ACCESS_KEY_ID\`
               - \`AWS_SECRET_ACCESS_KEY\`
            
            2. **Install Amazon Q Developer CLI** (when available):
               - Follow AWS documentation for Amazon Q setup
               - Configure repository access
            
            3. **Enable Amazon CodeWhisperer** for security scanning
            
            4. **Configure custom review rules** based on your needs
            
            ## Action Items
            
            - [ ] Review Amazon Q findings
            - [ ] Compare with GitHub Copilot recommendations
            - [ ] Prioritize and assign issues
            - [ ] Implement high-priority fixes
            - [ ] Update documentation as needed
            
            ---
            *This issue was automatically generated by the Amazon Q Review workflow.*
            `;
            
            // Check for existing Amazon Q review issues
            const issues = await github.rest.issues.listForRepo({
              owner: context.repo.owner,
              repo: context.repo.repo,
              state: 'open',
              labels: ['amazon-q', 'automated'],
              per_page: 10
            });
            
            const recentIssue = issues.data.find(issue => {
              const createdAt = new Date(issue.created_at);
              const daysSinceCreation = (Date.now() - createdAt) / (1000 * 60 * 60 * 24);
              return daysSinceCreation < 7;
            });
            
            if (recentIssue) {
              console.log(`Recent issue found: #${recentIssue.number}, updating`);
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: recentIssue.number,
                body: `## Updated Review (${date})\n\n${report}`
              });
            } else {
              await github.rest.issues.create({
                owner: context.repo.owner,
                repo: context.repo.repo,
                title: title,
                body: body,
                labels: ['amazon-q', 'automated', 'code-review', 'needs-review']
              });
            }

      - name: Upload Amazon Q Report
        uses: actions/upload-artifact@main
        with:
          name: amazonq-review-report
          path: |
            /tmp/amazonq-report.md
            /tmp/amazonq-prep.md
          retention-days: 90
        continue-on-error: true
