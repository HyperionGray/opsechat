# Testing
pytest>=7.0.0
pytest-cov>=4.0.0

# Linting / static analysis
flake8>=6.0.0
bandit>=1.7.0

# Utilities already in requirements.txt but listed here
# so a bare `pip install -r requirements-dev.txt` works
# without also installing the main file first.
PyYAML>=6.0
