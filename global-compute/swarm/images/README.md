# Images

- **OSv / Capstan**: boots `guest_init.sh` (daemon loop).
- **Ubuntu cloud-init**: installs container runtime; starts agent.

**TODO**
- [ ] Document image build pipelines.
- [ ] Versioning & SBOM tracking.
