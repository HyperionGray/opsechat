# 🌊🔥💀 Genesis Worker Configuration 💀🔥🌊

name = "genesis-worker"
main = "genesis_worker.js"
compatibility_date = "2024-01-01"

# KV namespace for edge state
kv_namespaces = [
  { binding = "GENESIS_KV", id = "47a79ebeda2e42ae8afaaa48bb8f1e8f" }
]

# Cron trigger - runs every minute on all edges
[triggers]
crons = ["* * * * *"]

# Routes - customize these for your domain
# routes = [
#   { pattern = "genesis.yourdomain.com/*", zone_name = "yourdomain.com" }
# ]

# Workers KV storage for edge state persistence
# Run: wrangler kv:namespace create "GENESIS_KV" --preview
# Then update the IDs above

[env.production]
name = "genesis-worker"
# Add production-specific config here

[env.staging]
name = "genesis-worker-staging"
# Add staging-specific config here
