# Wrangler Config

- Bindings: KV (GENESIS_KV, PCPU_STATE), R2 (artifacts), DO classes.
- `wrangler-genesis.toml` lives at repo root or here (document path).

**TODO**
- [ ] Template per-environment (dev/stage/prod).
