name: opsechat-mvp

services:
  tor:
    image: opsechat-tor:local
    build:
      context: .
      dockerfile: containers/tor.Dockerfile
    container_name: opsechat-tor
    command: ["tor", "-f", "/etc/tor/torrc"]
    volumes:
      - ./containers/torrc:/etc/tor/torrc:ro
      - tor-data:/var/lib/tor
    networks:
      - opsechat-network
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    healthcheck:
      test: ["CMD", "nc", "-z", "127.0.0.1", "9051"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  opsechat:
    build:
      context: .
      dockerfile: containers/Dockerfile
    container_name: opsechat-app
    depends_on:
      tor:
        condition: service_healthy
    environment:
      TOR_CONTROL_PORT: "9051"
      TOR_CONTROL_HOST: tor
      FLASK_DEBUG: "0"
      PYTHONDONTWRITEBYTECODE: "1"
    volumes:
      - tor-data:/var/lib/tor:ro
    # No host ports by default. Use the onion service printed in logs.
    # For localhost debugging only, uncomment:
    # ports:
    #   - "127.0.0.1:5000:5000"
    tmpfs:
      - /tmp:size=64m,mode=1777
    read_only: true
    networks:
      - opsechat-network
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    healthcheck:
      test: ["CMD", "curl", "--fail", "--silent", "http://127.0.0.1:5000/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s
    stdin_open: true
    tty: true

networks:
  opsechat-network:
    driver: bridge

volumes:
  tor-data:
