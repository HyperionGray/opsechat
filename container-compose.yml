name: opsechat-mvp

services:
  admin-proxy:
    image: opsechat-admin-proxy:local
    build:
      context: .
      dockerfile: containers/proxy.Dockerfile
    container_name: opsechat-admin-proxy
    depends_on:
      opsechat:
        condition: service_healthy
    ports:
      - "127.0.0.1:8080:8080"
    networks:
      - admin-network
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    healthcheck:
      test: ["CMD", "wget", "-q", "-O", "-", "http://127.0.0.1:8080/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 5s

  tor:
    image: opsechat-tor:local
    build:
      context: .
      dockerfile: containers/tor.Dockerfile
    container_name: opsechat-tor
    command: ["tor", "-f", "/etc/tor/torrc"]
    volumes:
      - ./containers/torrc:/etc/tor/torrc:ro
      - tor-data:/var/lib/tor
    networks:
      - opsechat-network
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    healthcheck:
      test: ["CMD", "nc", "-z", "127.0.0.1", "9051"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  opsechat:
    build:
      context: .
      dockerfile: containers/Dockerfile
    container_name: opsechat-app
    depends_on:
      tor:
        condition: service_healthy
    environment:
      TOR_CONTROL_PORT: "9051"
      TOR_CONTROL_HOST: tor
      TOR_SOCKS_PORT: "9050"
      TOR_SOCKS_HOST: tor
      OPSECHAT_REQUIRE_TOR: "1"
      OPSECHAT_FORCE_TOR_EGRESS: "1"
      # The hidden-service target is interpreted by the Tor process. Tor
      # lives in the `tor` container; the Flask app lives here in
      # `opsechat`. Both share the `opsechat-network` bridge, so the Tor
      # container can reach us as `opsechat:5000`. Without this, Tor
      # would forward onion traffic to its own 127.0.0.1:5000, where
      # nothing is listening, and clients see "site not reachable".
      OPSECHAT_HS_TARGET_HOST: opsechat
      OPSECHAT_HS_TARGET_PORT: "5000"
      FLASK_DEBUG: "0"
      PYTHONDONTWRITEBYTECODE: "1"
    volumes:
      - tor-data:/var/lib/tor:ro
    # No app host ports by default. Use the onion service or the localhost-only
    # admin proxy on 127.0.0.1:8080.
    # For direct localhost debugging only, uncomment:
    # ports:
    #   - "127.0.0.1:5000:5000"
    tmpfs:
      - /tmp:size=64m,mode=1777
    read_only: true
    networks:
      - admin-network
      - opsechat-network
    restart: on-failure
    healthcheck:
      test: ["CMD", "curl", "--fail", "--silent", "http://127.0.0.1:5000/health"]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 20s
    stdin_open: true
    tty: true

networks:
  admin-network:
    driver: bridge
  opsechat-network:
    driver: bridge

volumes:
  tor-data:
